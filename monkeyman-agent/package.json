{
  "name": "monkeyman-claude-agent",
  "version": "1.0.0",
  "description": "Claude Sonnet 4.6 agent for MonkeyMan operations — drop-in replacement for the n8n workflow.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^0.8.0",
    "dotenv": "^16.6.1",
    "express": "^4.21.1"
  }
}
